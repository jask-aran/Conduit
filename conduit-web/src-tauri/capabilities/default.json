{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "What the packaged window may ask the shell to do. Grants are added as features land; general shell execution, arbitrary filesystem access and remote-page IPC are never on this list.",
  "windows": [
    "main"
  ],
  "permissions": [
    "core:default",
    "core:webview:allow-set-webview-zoom",
    "updater:default",
    "process:allow-restart",
    "global-shortcut:default"
  ]
}
